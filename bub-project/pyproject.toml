[project]
name = "bub-project"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "bub",
    "bub-tapestore-sqlalchemy",
]

[tool.uv.sources]
bub = { path = "..", editable = true }
bub-tapestore-sqlalchemy = { path = "../vendor/bub-tapestore-sqlalchemy", editable = true }
